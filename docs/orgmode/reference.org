#+title: Reference
#+options: toc:nil num:nil

* Command line

| Verb | Habitat | Does |
|---+---+---|
| =remember TEXT= | pack | one lesson, stored as written |
| =prefer TEXT= | pack | one standing preference |
| =forget ID [--why DEED]= | pack | tombstone a claim, naming the deed that withdrew it |
| =search QUERY= | pack | ranked claims |
| =island CUE= | pack | the memories a task activates: search hits spread along the links |
| =due= | pack | claims whose review clock has run out |
| =graded ID [--lapsed]= | pack | one review graded |
| =trust FROM TO WEIGHT [--why DEED]...= | pack | one trust row |
| =learn ID --outcome OPTION [--beta B]= | tracker, pack | reweigh voters by what turned out right |
| =evidence ACCESSION= / =current ACCESSION= | deed store | intact; still the tip |
| =deed ID [--add ACCESSION]= | tracker | cite a deed on a node, or list citations |
| =recall ID= | tracker | the working set |
| =vote ID [--for OPTION]= | tracker | cast, or read the tally |
| =consensus ID= | consensus, tracker | settle under the pack's trust rows |
| =claim ID --assignee NAME= | claim graph | a session node for a tracker id |
| =complete ID [--status S]= | claim graph | finish the session node |
| =cards [--dir DIR]= | cards | print =USER.md= and =MEMORY.md= |
| =policy ARGV...= | policy | print the line as it would run |
| =handover --out DIR [--project P]... [--issue I]...= | all | pack, seal, sign |
| =receive DIR [--since BRIDGE] [--import]= | all | check, and import the atoms |
| =doctor= | all | which habitats answer |

A tracker id maps to one claim-graph node (FNV-1a 128 of the id) and a name
to one actor; a 32-hex id passes through.

* Model Context Protocol (MCP) tools

Writers: =ljos_remember=, =ljos_prefer=, =ljos_forget=, =ljos_trust=,
=ljos_learn=, =ljos_graded=, =ljos_deed=, =ljos_vote=, =ljos_claim=,
=ljos_complete=, =ljos_handover=, =ljos_receive=. Readers: =ljos_search=, =ljos_island=,
=ljos_due=, =ljos_evidence=, =ljos_current=, =ljos_recall=,
=ljos_consensus=, =ljos_cards=, =ljos_policy=, =ljos_doctor=. Resources:
=ljos://cards/USER.md=, =ljos://cards/MEMORY.md=. Prompts:
=start_a_sitting=, =check_a_handover=.

* Environment

| Variable | Read by |
|---+---|
| =PACKSET_URL= | the pack client; required for pack verbs |
| =VISSUE_ROOT=, =VISSUE_AGENT= | the tracker |
| =DEEDAR_URL=, =DEEDAR_HOST_SIGNING_KEY= | the deed store; the key signs handovers |
| =CLAIMDAG_DIR= | the claim graph |
| =LJOS_CARDS_DIR= | the server's cards directory |

* The learning rule

After an outcome, every voter whose ballot it refuted shrinks in every
other voter's row by =beta= (default 0.5), floored at 0.01. A vindicated
voter keeps its weight, and a missing row starts at 1. Rows are written
complete, so the settle sees the whole graph. The update is the
multiplicative weights rule of Hedge (doi:10.1006/jcss.1997.1504).

* Crates

| Crate | Carries |
|---+---|
| =ljos-cli= | the library and =ljos= |
| =ljos-mcp= | the MCP server |
| =ljos-consensus= | DeGroot and Friedkin-Johnsen settles, Seldon export (separate repository) |
